# Copy to secrets.yaml (which you should NOT commit) before running esphome.
wifi_ssid: "your-wifi"
wifi_password: "your-wifi-password"
fallback_password: "setup-password"
ota_password: "ota-password"
# Generate with: openssl rand -base64 32
api_encryption_key: "REPLACE_ME_WITH_BASE64_32_BYTE_KEY"
