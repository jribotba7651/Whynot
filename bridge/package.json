{
  "name": "reverie-bridge",
  "version": "1.0.0",
  "description": "Always-on bridge that talks to a Reverie bed via an ESP32 BLE proxy and runs scheduled commands.",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsx watch src/index.ts",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@2colors/esphome-native-api": "^1.3.0",
    "fastify": "^4.28.1",
    "node-cron": "^3.0.3",
    "pino": "^9.0.0",
    "ws": "^8.17.1"
  },
  "devDependencies": {
    "@types/node": "^20.12.7",
    "@types/node-cron": "^3.0.11",
    "@types/ws": "^8.5.10",
    "tsx": "^4.16.2",
    "typescript": "^5.4.5"
  }
}
